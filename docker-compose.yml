services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: pulse
      POSTGRES_PASSWORD: pulse
      POSTGRES_DB: pulse_survey
    ports:
      - "5433:5432"
    volumes:
      - pulse_pg_data:/var/lib/postgresql/data

volumes:
  pulse_pg_data:
